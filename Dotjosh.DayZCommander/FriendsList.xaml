<UserControl x:Class="Dotjosh.DayZCommander.FriendsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:DayZCommander="clr-namespace:Dotjosh.DayZCommander" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<ItemsControl ItemsSource="{Binding Friends}">
			<ItemsControl.Resources>
				<DayZCommander:IsPlayingToColorConverter x:Key="IsPlayingToColorConverter"/>
			</ItemsControl.Resources>
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<TextBlock Text="{Binding Name}" 
							   FontSize="14"
							   FontWeight="Bold"
							   Foreground="{Binding IsPlaying, Converter={StaticResource IsPlayingToColorConverter}}"/>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
    </Grid>
</UserControl>
