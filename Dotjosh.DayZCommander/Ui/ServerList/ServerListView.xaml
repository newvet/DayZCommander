<UserControl x:Class="Dotjosh.DayZCommander.Ui.ServerList.ServerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ServerList="clr-namespace:Dotjosh.DayZCommander.Ui.ServerList" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="150"/>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		
		<ServerList:FiltersView DataContext="{Binding FiltersViewModel}"/>
		<ServerList:ListView DataContext="{Binding ListViewModel}"
							 HorizontalAlignment="Stretch"
							 VerticalAlignment="Stretch"
							 Grid.Column="1"/>
		
		
	</Grid>
</UserControl>
